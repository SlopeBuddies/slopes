import React, { Component } from 'react'

export class Login extends Component {
  render() {
    return (
      <div>
          <a href={procerss.env.REACT_APP_LOGIN}>
        <button> LOGIN </button>
        </a>
      </div>
    )
  }
}

export default Login
